# Gemini server secret. Never prefix this with VITE_ and never expose it to React.
GEMINI_API_KEY="replace-with-gemini-api-key"

# Firebase Admin credentials. Use GOOGLE_APPLICATION_CREDENTIALS for a local service
# account file, or provide the JSON as a secret in managed environments.
GOOGLE_APPLICATION_CREDENTIALS="C:\\path\\to\\firebase-service-account.json"
# FIREBASE_SERVICE_ACCOUNT_JSON="replace-with-json-secret"
FIREBASE_PROJECT_ID="gen-lang-client-0395567775"
FIREBASE_DATABASE_ID="ai-studio-cognivault-bb281c1f-91d0-45e0-8d8d-55493adb2b1d"

PORT="3000"
CORS_ORIGINS="http://localhost:3000"
APP_URL="http://localhost:3000"
